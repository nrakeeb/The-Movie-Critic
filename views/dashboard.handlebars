<section class="new-post-section">
    <button type="submit" class="button" id="create-new-post">+ Create new post</button>
</section>


{{#if posts.length}}
<section>
    <h1 class="your-dashboard">Your Dashboard</h1>
    <ul class="post-list">
        {{#each posts as |post|}}
        <li>
            {{> post-info post}}
            <a id="button" href="/dashboard/edit/{{post.id}}" class="edit-link">edit post</a>
        </li>
        {{/each}}
    </ul>
</section>
{{/if}}

<script src="/javascript/create-post.js"></script>